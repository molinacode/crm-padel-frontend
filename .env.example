# Frontend CRM. Ya no hay claves de Supabase en el navegador.
# En local, Vite hace proxy de /api y /fotos-alumnos a crm-padel-backend (:3001).
# Copia a ".env" solo si hace falta algo extra; el login va por cookie httpOnly.

# Vacío a propósito: no pongas VITE_SUPABASE_*.
